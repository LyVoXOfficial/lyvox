# 🚗 Vehicle Sync - Быстрый старт

> **⚡ 5-минутное руководство** по синхронизации автомобилей >= 1980 года

---

## ✅ Что уже готово

Созданы и готовы к использованию:

1. ✅ **analyze-vehicle-sync.mjs** - анализ JSON vs БД
2. ✅ **check-vehicle-duplicates.mjs** - проверка дубликатов
3. ✅ **generateVehicleSeed.mjs** - генератор с фильтром >= 1980
4. ✅ **sync-vehicles-master.mjs** - мастер-скрипт (всё в одном)
5. ✅ **VEHICLE_SYNC_GUIDE.md** - полная документация

---

## 🚀 Три команды для полной синхронизации

### 1️⃣ Подготовка (один раз)

```bash
# Настройте переменные окружения в .env или экспортируйте:
export DATABASE_URL="postgresql://postgres:PASSWORD@HOST:5432/postgres"
```

### 2️⃣ Автоматическая синхронизация

```bash
# Запустите мастер-скрипт (всё в одном):
node scripts/sync-vehicles-master.mjs --apply
```

Скрипт выполнит:

- ✅ Анализ JSON vs БД
- ✅ Проверку дубликатов
- ✅ Генерацию vehicles_seed.sql (>= 1980)
- ✅ Применение к БД (после подтверждения)

### 3️⃣ Переводы (опционально, требует AI API ключи)

```bash
# Если нужны переводы на все языки:
export GOOGLE_API_KEY="your-key"
node scripts/vehicle_i18n_normalize.mjs
node scripts/vehicle_i18n_expand.mjs
```

---

## 📊 Ожидаемый результат

После выполнения вы получите:

```
✅ ~245 марок (>= 1980)
✅ ~3400 моделей (>= 1980)
✅ ~8900 поколений
✅ Полная информация (pros, cons, tips)
✅ Без дубликатов
```

---

## 🎯 Что дальше?

После успешной синхронизации:

1. **Проверьте результат:**

   ```bash
   node scripts/analyze-vehicle-sync.mjs
   ```

2. **Проверьте на сайте:**
   - Откройте форму создания объявления
   - Проверьте dropdown с марками/моделями
   - Убедитесь, что данные отображаются корректно

3. **Если нужны переводы:**
   - См. раздел "Переводы" в VEHICLE_SYNC_GUIDE.md
   - Требуется OPENAI_API_KEY или GOOGLE_API_KEY

---

## 🐛 Если что-то пошло не так

### Проблема 1: "DATABASE_URL is required"

```bash
export DATABASE_URL="postgresql://..."
```

### Проблема 2: Обнаружены дубликаты

```bash
# Запустите диагностику:
node scripts/check-vehicle-duplicates.mjs
# Удалите дубликаты через Supabase Dashboard
```

### Проблема 3: Нужна детальная информация

```bash
# Читайте полную документацию:
cat docs/development/VEHICLE_SYNC_GUIDE.md
```

---

## 📝 Пошаговый режим (если нужен контроль)

Вместо автоматического режима можете запускать по шагам:

```bash
# Шаг 1: Анализ
node scripts/analyze-vehicle-sync.mjs

# Шаг 2: Проверка дубликатов
node scripts/check-vehicle-duplicates.mjs

# Шаг 3: Генерация
node scripts/generateVehicleSeed.mjs

# Шаг 4: Проверьте файл
head -n 50 vehicles_seed.sql

# Шаг 5: Применение
node scripts/runSeed.mjs ./vehicles_seed.sql

# Шаг 6: Финальная проверка
node scripts/analyze-vehicle-sync.mjs
```

---

## 💡 Полезные флаги

```bash
# Dry run (показать что будет сделано)
node scripts/sync-vehicles-master.mjs --dry-run

# Пропустить анализ и проверки (быстро)
node scripts/sync-vehicles-master.mjs --skip-analysis --skip-duplicates --apply

# Только анализ (безопасно)
node scripts/analyze-vehicle-sync.mjs
```

---

## 📚 Дополнительные ресурсы

- **Полная документация:** `docs/development/VEHICLE_SYNC_GUIDE.md`
- **Исходный JSON:** `seed/vehicles_full_enriched2.json`
- **Сгенерированный SQL:** `vehicles_seed.sql` (после генерации)
- **Логи переводов:** `logs/vehicle_i18n_*.csv`

---

## ⏱️ Время выполнения

- Анализ: ~30 секунд
- Проверка дубликатов: ~20 секунд
- Генерация seed: ~2 минуты
- Применение к БД: ~5-15 минут
- Переводы: ~30-60 минут (с AI)

**Итого: ~20 минут** (без переводов)

---

**✅ Готово! Вопросы? См. VEHICLE_SYNC_GUIDE.md**

---

## 🔗 Related Docs

**Development:** [VEHICLE_SYNC_GUIDE.md](./VEHICLE_SYNC_GUIDE.md) • [VEHICLE_DATA_COMPLETION.md](./VEHICLE_DATA_COMPLETION.md) • [VEHICLE_SYNC_FINAL_REPORT.md](./VEHICLE_SYNC_FINAL_REPORT.md)
**Catalog:** [CATALOG_IMPLEMENTATION_STATUS.md](../catalog/CATALOG_IMPLEMENTATION_STATUS.md) • [CATALOG_MASTER.md](../catalog/CATALOG_MASTER.md)
